// NOTE: Abbreviated way to declare a class (use this to declare classes)
class Car {
    constructor(
        public make: string,
        public model: string,
        public readonly year: number
    ) { }
}
